# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — NEVER commit. Public repo.
.env
*.env
# Updater signing keys (private half lives outside the repo + as a CI secret)
*.key
*.pem
.tauri/

# Claude config (private, symlinked to membeme)
CLAUDE.md
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
